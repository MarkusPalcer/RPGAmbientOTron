<UserControl x:Class="AmbientOTron.Views.SoundBoard.SoundBoardPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:soundBoard="clr-namespace:AmbientOTron.Views.SoundBoard"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
             Padding="5"
             d:DataContext="{d:DesignInstance soundBoard:SoundBoardPropertiesViewModel}">
    <StackPanel Orientation="Vertical" DataContext="{Binding ViewModel}">
        <TextBlock FontSize="16">Sound board properties</TextBlock>
        <Label>Name:</Label>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
