<UserControl x:Class="AmbientOTron.Views.Gaming.SoundBoard.SoundBoardPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AmbientOTron.Views.Gaming.SoundBoard"
             mc:Ignorable="d" 
             MinWidth="300"
             d:DesignHeight="300" d:DesignWidth="300"
             Padding="5,5,10,5"
             d:DataContext="{d:DesignInstance local:SoundBoardPropertiesViewModel}">
    <UserControl.Resources>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Margin" Value="-5,10,0,0" />
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical" DataContext="{Binding ViewModel}">
        <TextBlock FontSize="16">Sound board properties</TextBlock>
        <Label>Name:</Label>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
